%h1 Create a new post.

= form_for :post, :url => posts_path do |form|
  = form.error_messages

  %p
    = form.label :title, 'title'
    = form.text_field :title

  %p
    = form.label :text, 'text'
    = form.text_area :text

  %p
    = form.submit "post to blog"

